---
title: WebSocket overview
description: Enterprise WebSocket — connection and protocol
---

The **Live Data WebSocket API** group in the sidebar documents `GET /ws` on `wss://api.hypedexer.com` and the JSON **components/schemas** (client messages, server messages, fills, books, etc.).
