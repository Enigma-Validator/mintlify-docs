---
title: Live data
description: Real-time Hyperliquid data — REST
---

# Live data

**Live data** is served by **REST** — `POST /info` on the api host with typed JSON bodies (`metaAndAssetCtxs`, `allMids`, fills, order books, proxied HL types, etc.).

For **indexed / historical** queries use the **Historical data** tab.
