---
title: EVM
description: EVM blockchain data
---

HypeDexer indexes **HyperEVM** blockchain data — blocks, transactions, event logs, ledger transfers, and bridge events.

Use the **REST API** section below to query EVM indexed data on `api.hypedexer.com`.
